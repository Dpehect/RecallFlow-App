"use client";
import { CATEGORIES, LANGUAGES, LEVELS, type CategoryId, type LanguageId, type Level } from "../lib/curriculum";
export function DeckControls({ language, level, category, onLanguage, onLevel, onCategory }: { language: LanguageId; level: Level; category: CategoryId; onLanguage: (x: LanguageId) => void; onLevel: (x: Level) => void; onCategory: (x: CategoryId) => void }) {
  return <div className="deck-controls"><label><span>DİL</span><select aria-label="Çalışma dili" value={language} onChange={e => onLanguage(e.target.value as LanguageId)}>{LANGUAGES.map(x => <option key={x.id} value={x.id}>{x.label}</option>)}</select></label><label><span>SEVİYE</span><select aria-label="Dil seviyesi" value={level} onChange={e => onLevel(e.target.value as Level)}>{LEVELS.map(x => <option key={x} value={x}>{x}</option>)}</select></label><label><span>KATEGORİ</span><select aria-label="Kelime kategorisi" value={category} onChange={e => onCategory(e.target.value as CategoryId)}>{CATEGORIES.map(x => <option key={x.id} value={x.id}>{x.label}</option>)}</select></label><div className="deck-total"><b>100</b><span>KART</span></div></div>;
}
