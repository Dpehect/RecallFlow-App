"use client";
import { useEffect, useMemo, useState } from "react";
import Link from "next/link";
import { SiteNav } from "../../components/SiteNav";
import { EMPTY_DATA, dayKey, loadLearning, streak, type LearningData } from "../../lib/learning";
export default function ProgressPage(){const[data,setData]=useState<LearningData>(EMPTY_DATA);useEffect(()=>{const update=()=>setData(loadLearning());update();window.addEventListener("recallflow:update",update);return()=>window.removeEventListener("recallflow:update",update)},[]);const metrics=useMemo(()=>{const now=Date.now(),reviews=Object.values(data.reviews),today=data.history.filter(x=>dayKey(x.at)===dayKey(now)),good=today.filter(x=>x.rating==="good").length,due=reviews.filter(x=>x.dueAt<=now).length,accuracy=Math.round(today.filter(x=>x.rating==="good").length/Math.max(1,today.length)*100);return{learned:reviews.filter(x=>x.repetitions>0).length,due,today:today.length,accuracy,streak:streak(data.history),remaining:Math.max(0,10-today.length),good}},[data]);return <main><SiteNav/><header className="page-head progress-title"><span className="eyebrow">03 / İLERLEME</span><h1>Rakam değil.<br/><em>Ritim.</em></h1><p>Temsili istatistik yok. Her metrik gerçek kart kararlarından ve gerçek tekrar zamanlarından hesaplanır.</p></header><section className="progress-shell"><div className="score-card"><span>BUGÜNKÜ DOĞRULUK</span><b>{metrics.accuracy}<i>%</i></b><div><i style={{width:`${metrics.accuracy}%`}}/></div><p>{metrics.today?`${metrics.today} gerçek tekrar üzerinden hesaplandı.`:"İlk oturumundan sonra skor oluşacak."}</p></div><div className="progress-stats"><article><span>ÖĞRENİLEN</span><b>{metrics.learned}</b><p>En az bir başarılı tekrarı olan kart.</p></article><article><span>BUGÜN</span><b>{metrics.today}</b><p>Bugün verdiğin toplam kart kararı.</p></article><article><span>TEKRAR BEKLİYOR</span><b>{metrics.due}</b><p>Zamanı gelmiş gerçek tekrar kuyruğu.</p></article><article className="streak"><span>AKTİF SERİ</span><b>{metrics.streak}<i> gün</i></b><p>Arka arkaya çalışılan takvim günü.</p></article></div><div className="next-action"><div><span className="eyebrow">TEK SONRAKİ ADIM</span><h2>{metrics.remaining?`${metrics.remaining} kart kaldı.`:"Hedef tamamlandı."}</h2><p>{metrics.due?`${metrics.due} kart tekrar için hazır.`:"Yeni bir deste seçebilirsin."}</p></div><Link href="/learn">OTURUMA DÖN ↗</Link></div></section></main>}
