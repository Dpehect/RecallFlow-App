"use client";
import { useState } from "react";
import Link from "next/link";
import { SiteNav } from "../../components/SiteNav";
const methods=[{n:"01",tag:"AKTİF HATIRLAMA",title:"Cevaptan önce sen varsın.",text:"Kartın anlamını hemen göstermek öğrenme hissi verir ama hafıza oluşturmaz. Ön yüz, cevabı zihninden üretmen için bilinçli bir boşluk bırakır."},{n:"02",tag:"ARALIKLI TEKRAR",title:"Zor olan erken döner.",text:"Tekrar, zor ve bildim kararları kuyruğu değiştirir. Öğrenci zamanını zaten bildiği kartlara harcamaz."},{n:"03",tag:"ÇOKLU KODLAMA",title:"Gör, duy, sonra yaz.",text:"Aynı kelime görsel kart, ses ve üretim pratiğiyle farklı hafıza yollarına bağlanır. Üç mod aynı öğrenme kaydını işler."},{n:"04",tag:"BAĞLAMSAL KAPANIŞ",title:"Kelime yalnız bırakılmaz.",text:"Doğru cevap örnek cümleyle kapanır. Böylece biçim, anlam ve kullanım aynı anda pekişir."}];
export default function MethodPage(){const[active,setActive]=useState(0);const item=methods[active];return <main className="screen-page"><SiteNav/><section className="method-console"><header><span className="eyebrow">04 / METOT</span><h1>Tanıma değil.<br/><em>Geri çağırma.</em></h1><p>Dört ilke. Uzun teori yok; her biri doğrudan çalışma ekranında karşılığını bulur.</p></header><div className="method-tabs" role="tablist">{methods.map((x,i)=><button role="tab" aria-selected={active===i} className={active===i?"active":""} key={x.n} onClick={()=>setActive(i)}><b>{x.n}</b><span>{x.tag}</span></button>)}</div><article className="method-focus"><span>{item.n} / {item.tag}</span><h2>{item.title}</h2><p>{item.text}</p><Link href="/learn">10 KART BAŞLAT ↗</Link></article></section></main>}
